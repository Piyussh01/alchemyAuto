# --- LiveKit voice agent ---
livekit-agents[deepgram,cartesia,silero,turn-detector,anthropic]~=1.6
livekit-plugins-noise-cancellation~=0.2   # optional: background-noise / echo cancellation
livekit-api                                # token server (LiveKit JWT signing)

# --- RAG (Codex retrieval) ---
llama-index~=0.14
llama-index-embeddings-openai

# --- Tools ---
tavily-python                              # web_search tool
supabase                                   # hosted Postgres client (inventory + test_drives)

# --- Token server (FastAPI) ---
fastapi
uvicorn[standard]

# --- Misc ---
python-dotenv                              # loads .env
reportlab  